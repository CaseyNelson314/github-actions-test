name: Run Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Build
        uses: actions/checkout@v2
        run: |
          g++ -o out ./src/main.cpp
          ./out

